import os, sys, time
import numpy as np

def generate_base():
    # set the base array shape (h,w)
    shape = (5,5)
    
    # create a array with 0/1 random in each cell
    array = np.random.randint(2,size=shape)
    
    # display it
    os.system('clear')
    print(array)
    return array
    time.sleep(1)
    

def transform(current_array):
    for 
    
    


def main():
    array=generate_base()


main()
